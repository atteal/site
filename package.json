{
  "name": "atteal",
  "version": "0.0.1",
  "description": "",
  "main": "build.js",
  "scripts": {
    "build": "node -r dotenv/config build.js dotenv_config_path=production.config.js && npm run build-css",
    "build-css": "node-sass -o public/assets/styles app/assets/styles",
    "build:dev": "DEBUG=* node -r dotenv/config build.js",
    "dev": "npm run build:dev && npm run build-css"
  },
  "author": "Ben Cooper",
  "dependencies": {
    "contentful-metalsmith": "^0.8.0",
    "metalsmith": "^2.3.0",
    "metalsmith-layouts": "^1.7.0",
    "metalsmith-permalinks": "^0.5.0",
    "node-sass": "^4.5.0",
    "pug": "^2.0.0-beta6"
  },
  "devDependencies": {
    "dotenv": "^4.0.0",
    "metalsmith-debug": "^1.1.0",
    "metalsmith-markdown": "^0.2.1"
  }
}
